# Security

Static checks are not a security guarantee. Review package recipes before execution and keep contributor builds separate from signing/promotion. Report reproducible problems without credentials or private user data through the repository support route.
